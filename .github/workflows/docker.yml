name: Build and Push Docker Image

on:
  push:
    branches: [ main, master ]
    tags: [ 'v*' ]
  workflow_dispatch:
    inputs:
      version:
        description: 'EasyTier Version'
        required: true
        default: '2.4.5'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Extract version metadata
      id: meta
      run: |
        if [ -n "${{ github.event.inputs.version }}" ]; then
          VERSION="${{ github.event.inputs.version }}"
        elif [[ ${{ github.ref }} == refs/tags/* ]]; then
          VERSION=${GITHUB_REF#refs/tags/v}
        else
          VERSION="latest"
        fi
        echo "version=$VERSION" >> $GITHUB_OUTPUT

    - name: Build and push
      uses: docker/build-push-action@v5
      with:
        context: .
        platforms: linux/amd64
        push: true
        tags: |
          ${{ secrets.DOCKERHUB_USERNAME }}/easytier-mange:latest
          ${{ secrets.DOCKERHUB_USERNAME }}/easytier-mange:${{ steps.meta.outputs.version }}
        build-args: |
          EASYTIER_VERSION=${{ github.event.inputs.version || '2.4.5' }}

  # 移除有问题的测试步骤，或者修复它
  # test:
  #   runs-on: ubuntu-latest
  #   steps:
  #   - name: Test EasyTier
  #     run: |
  #       # 如果需要测试，应该下载正确的二进制文件
  #       curl -L -o easytier.zip https://github.com/EasyTier/EasyTier/releases/download/v2.4.5/easytier-linux-x86_64-v2.4.5.zip
  #       unzip easytier.zip
  #       cd easytier-linux-x86_64
  #       ./easytier-core --version || echo "测试完成"
